require 'spec_helper'

describe Ruboty::Bash do
  it 'has a version number' do
    expect(Ruboty::Bash::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
